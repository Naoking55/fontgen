# 偏旁自動検出実験プログラム

## 概要

このプログラムは、漢字から偏旁を自動的に検出・抽出する実験用ツールです。
実用レベルに達したら本体のフォントエディタに統合する予定です。

## バージョン情報

### v0.2.0 (2025-11-06) - 辞書ベースアプローチの実装 ✨NEW
- **Unicode IDS (Ideographic Description Sequences) データベース**を統合
- **辞書ベース検出**: 文字構造情報を使用した偏旁抽出
- **康熙部首データベース**: 214部首と異体字対応
- **改良版共通偏旁抽出**: 複数文字から同じ偏旁を持つものを分析
- v0.1.0の連結成分分析も継続サポート

### v0.1.0 (2025-11-06) - 初期実験版
- 連結成分分析による基本的な偏旁分割
- テンプレートマッチング基盤
- 複数文字からの共通部分抽出基盤

## 実装されている手法

### 🆕 A. 辞書ベース検出 (v0.2.0)
- **IDS (Ideographic Description Sequences)** を使用
  - Unicode標準の文字構造記述
  - ⿰(左右), ⿱(上下), ⿵(包囲) など12種類の演算子
  - 約97,000文字の構造データ（CJKVI IDSプロジェクト）
- **利点**:
  - 構造情報に基づく正確な分割位置特定
  - 接触している偏旁も分離可能（理論上）
  - まだれ、もんがまえ等の複雑な構造にも対応
- **欠点**:
  - IDSデータが存在しない文字は処理不可
  - 実際の画像抽出は構造情報の解釈精度に依存

### B. 連結成分分析 (Connected Component Analysis)
- 文字を独立した塊（成分）に分解
- 各成分の位置・サイズから偏旁タイプを推定
- **利点**: シンプルで高速、縦書き/横書き偏旁の分離に有効
- **欠点**: 接触している偏旁は分離困難

### C. 複数文字からの共通偏旁抽出 (改良版 v0.2.0)
- IDS構造情報を使用して共通構成要素を特定
- 同じ偏旁を持つ複数の文字を比較
- 例: 海・江・河 → さんずい (氵) を抽出
- **利点**:
  - 自動的に偏旁を発見可能
  - 人手のアノテーション不要
  - 画像処理とセマンティック情報の融合
- **欠点**: 複数の類似文字が必要

### D. テンプレートマッチング (準備済み)
- 既知の偏旁をテンプレートとして登録
- 正規化相互相関(NCC)でマッチング
- スケール変換に対応
- **利点**: 既知の偏旁を高精度で検出
- **欠点**: 未知の偏旁は検出不可

## インストール

### 必要なライブラリ

```bash
pip install pillow numpy scipy
```

## 使い方

### v0.2.0の新機能

#### 1. 辞書ベース検出を使う

```bash
python radical_auto_detect_experiment_v02.py
```

1. **フォント読み込み**
   - メニュー → ファイル → フォントを開く

2. **文字を入力してレンダリング**
   - 「文字」欄に漢字を1文字入力（例: 語、魔、門）
   - 「レンダリング」ボタンをクリック
   - ステータスバーにIDS情報が表示される

   例: 語 → IDS: ⿰言吾, 構造: left_right

3. **辞書ベース検出を実行**
   - 「🔍 辞書ベース検出」ボタンをクリック
   - 構造情報に基づいて自動的に偏旁を抽出

4. **結果の確認と保存**
   - 右側に抽出された偏旁が表示される
   - 各偏旁の「この成分を保存」ボタンで保存

**対応する文字構造:**
- ⿰ 左右配置 (例: 語、明、海)
- ⿱ 上下配置 (例: 空、字、雲)
- ⿵ 上三方包囲 (例: 門、問、間)
- ⿸ 左上包囲（まだれ等） (例: 広、店、魔)
- その他の複雑な構造

#### 2. 複数文字から共通偏旁を抽出

1. **複数文字を入力**
   - 「複数文字から共通偏旁抽出」セクション
   - 「文字列」欄に文字を入力（例: 海江河）

2. **共通偏旁を抽出**
   - 「共通偏旁を抽出」ボタンをクリック
   - IDS情報から共通構成要素を特定
   - 各文字から該当部分を抽出して表示

**推奨テストケース:**
- さんずい: 海江河
- にんべん: 信住仁
- きへん: 林村校
- ごんべん: 語話説

#### 3. 連結成分分析（v0.1.0互換）

1. **文字をレンダリング**

2. **パラメータ調整**
   - **2値化閾値**: 文字の境界を決める閾値（デフォルト200）
   - **最小サイズ**: 成分として認識する最小ピクセル数（デフォルト100）

3. **分析実行**
   - 「🧩 連結成分分析」ボタンをクリック

### v0.1.0の使い方（従来版）

```bash
python radical_auto_detect_experiment.py
```

基本的な流れは同じですが、辞書ベース検出は含まれていません。

## 実験の進め方

### 🆕 Phase 0: 辞書ベース検出の検証 (v0.2.0)

v0.1.0で失敗したケースを中心にテスト

#### 実験A: 複雑な偏旁（まだれ等）
```
テスト文字: 広、魔、慶、店、庫
期待結果: まだれ（广）と内部の成分に分割
IDS構造: ⿸ (左上包囲)
```

**v0.1.0の問題**: 画数が多いと連結成分分析が失敗
**v0.2.0の改善**: IDS情報で構造を特定し、領域ベースで抽出

#### 実験B: 枠構造（もんがまえ等）
```
テスト文字: 間、問、門、閉、開
期待結果: 門構造と内部の成分に分割
IDS構造: ⿵ (上三方包囲)
```

**v0.1.0の問題**: 左右の柱が別々の成分として検出される
**v0.2.0の改善**: IDS演算子⿵で門構造を認識

#### 実験C: 囲み構造（くにがまえ等）
```
テスト文字: 国、囲、図、園
期待結果: 囗（くにがまえ）と内部の成分に分割
IDS構造: ⿴ (全包囲)
```

**v0.1.0の問題**: 内部成分が個別に分離される
**v0.2.0の改善**: 包囲構造を認識して適切に分割

#### 実験D: 接触する偏旁（さんずい等）
```
テスト文字: 海、江、河、池、湖
期待結果: さんずい（氵）と旁に分割
IDS構造: ⿰ (左右配置)
複数文字抽出テスト
```

**v0.1.0の問題**: 接触部分で分離困難、個別検出精度低
**v0.2.0の改善**:
- IDS情報で左右分割位置を特定
- 複数文字（海江河）から共通部分として氵を抽出

### Phase 1: 基本的な分割の検証

#### 実験1: 左右分割（偏・旁）
```
テスト文字: 語、話、時、明、晴、校、林、村
期待結果: 左成分と右成分に分割される
IDS構造: ⿰ (左右配置)
```

**確認ポイント**:
- 2成分に分割されるか？
- 左成分と右成分が正しく識別されるか？
- 接触部分で分離失敗していないか？

#### 実験2: 上下分割（冠・脚）
```
テスト文字: 空、家、字、安、宝、雲、雪
期待結果: 上成分と下成分に分割される
IDS構造: ⿱ (上下配置)
```

**確認ポイント**:
- 2成分に分割されるか？
- 上下の位置が正しく識別されるか？

#### 実験3: 複雑な構造
```
テスト文字: 森、品、晶、轟
期待結果: 3成分以上に分割される
IDS構造: ⿲ (左中右配置) または ⿳ (上中下配置)
```

**確認ポイント**:
- 繰り返しパターンが正しく認識されるか？

### Phase 2: パラメータのチューニング

#### チューニング項目

1. **2値化閾値の最適値を見つける**
   - フォントの太さによって最適値が異なる
   - 細いフォント: 180-200
   - 太いフォント: 200-220

2. **最小サイズの調整**
   - 小さすぎる: ノイズを誤検出
   - 大きすぎる: 小さな偏旁を見逃す
   - 推奨: 50-200の範囲で調整

### Phase 3: 成功パターンの蓄積

成功した分割結果を記録：

```json
{
  "character": "語",
  "font": "源ノ角ゴシック",
  "threshold": 200,
  "min_size": 100,
  "components": [
    {
      "position": "left",
      "saved_as": "言.png",
      "notes": "偏として使用可能"
    },
    {
      "position": "right",
      "saved_as": "吾.png",
      "notes": "旁として使用可能"
    }
  ],
  "success": true
}
```

## 判定基準

### 実用レベルの定義

以下の基準を満たせば本体統合を検討：

✅ **必須基準**:
1. 左右分割の成功率 > 80%（20文字中16文字以上）
2. 上下分割の成功率 > 70%（20文字中14文字以上）
3. 誤検出率 < 10%（間違った分割が10%未満）

✅ **推奨基準**:
1. パラメータ調整で複数のフォントに対応可能
2. 保存した偏旁が本体で正常に使用可能
3. ユーザーが手動で修正する必要が少ない

## 次のステップ

### 改善案

1. **エッジ検出の追加**
   - Cannyエッジ検出で境界を明確化
   - 接触部分の分離精度向上

2. **スケルトン化**
   - 文字のストロークを骨格化
   - 交点検出で分割位置を推定

3. **機械学習の導入**
   - CNNで偏旁領域をセグメンテーション
   - 学習データの収集が課題

### 本体統合計画

実用レベルに達した場合：

1. `FontEditorApp`にメニュー項目を追加
   - ツール → 偏旁自動検出

2. `GlyphEditor`から直接実行
   - 現在編集中の文字から偏旁を抽出
   - 抽出結果を偏旁パレットに自動登録

3. バッチ処理機能
   - 複数文字をまとめて処理
   - 共通偏旁を自動抽出

## トラブルシューティング

### Q: 成分が検出されない
A: 2値化閾値を下げてみてください（200 → 180など）

### Q: ノイズが大量に検出される
A: 最小サイズを上げてください（100 → 200など）

### Q: 接触部分で分離できない
A: 現在の連結成分分析では限界があります。次のフェーズでエッジ検出やストローク分析を導入予定

### Q: 特定のフォントで失敗する
A: フォントごとにパラメータを調整する必要があります。成功したパラメータをプリセットとして保存する機能を検討中

## ライセンス

本体のフォントエディタと同じライセンスを適用

## 貢献

実験結果のフィードバック歓迎：
- 成功した文字とパラメータ
- 失敗した文字と原因
- 改善提案

## v0.2.0の技術的詳細

### IDS (Ideographic Description Sequences) とは

IDSは、Unicode標準の一部として定義されている漢字の構造記述方法です。

#### IDS演算子の種類

| 演算子 | 意味 | 例 |
|--------|------|-----|
| ⿰ | 左右配置 | 語 = ⿰言吾 |
| ⿱ | 上下配置 | 字 = ⿱宀子 |
| ⿲ | 左中右3分割 | 弁 = ⿲廾、廾 |
| ⿳ | 上中下3分割 | 意 = ⿳立日心 |
| ⿴ | 全包囲 | 国 = ⿴囗玉 |
| ⿵ | 上三方包囲 | 門 = ⿵門、 |
| ⿶ | 下三方包囲 | 凶 = ⿶凵メ |
| ⿷ | 左三方包囲 | 匠 = ⿷匚斤 |
| ⿸ | 左上包囲 | 広 = ⿸广黄 |
| ⿹ | 右上包囲 | 勿 = ⿹勹、 |
| ⿺ | 左下包囲 | 廷 = ⿺廴壬 |
| ⿻ | 重ね | 凹 = ⿻口一 |

### データソース

- **IDS Database**: [CJKVI IDS Project](https://github.com/cjkvi/cjkvi-ids)
  - 約97,000文字の構造データ
  - 初回起動時に自動ダウンロード
  - ローカルキャッシュ: `ids_cache.json`

- **康熙部首**: 214部首とよく使われる異体字を内蔵

### アルゴリズムの改善点

#### 1. 構造解析の精度向上
- **v0.1.0**: 画像ベース → ピクセルの連結性のみで判断
- **v0.2.0**: 辞書+画像 → 文字の意味構造を理解した上で抽出

#### 2. 複雑な構造への対応
- **まだれ（⿸）**: L字型の包囲構造を正しく認識
- **もんがまえ（⿵）**: 門の左右柱を1つの成分として扱う
- **くにがまえ（⿴）**: 内側の要素を1つのまとまりとして抽出

#### 3. 共通偏旁抽出の高度化
- IDS情報から共通構成要素を特定
- 複数文字の画像から該当領域を抽出
- セマンティック情報とピクセル情報の融合

### 既知の制限事項

1. **IDSデータの制約**
   - すべての漢字にIDS情報があるわけではない
   - 特に古い字体や異体字で欠落がある

2. **画像抽出の精度**
   - IDS構造は理想的な分割を示すが、実際のフォントでは重なりや接触がある
   - 領域ベースの抽出は近似的

3. **フォント依存性**
   - フォントによって字形が異なる
   - 旧字体と新字体で構造が変わる場合がある

### 今後の拡張予定

1. **Unihan kRSUnicodeフィールドの統合**
   - 部首・画数情報をより正確に取得
   - `unihan-etl`ライブラリの利用

2. **機械学習による境界検出**
   - IDS情報で大まかな領域を特定
   - CNNでピクセルレベルの境界を精密に検出

3. **旧字体・新字体の対応マッピング**
   - 異体字データベースの統合
   - 構造の変化に対応した柔軟な抽出

---

**バージョン**: 0.2.0
**最終更新**: 2025-11-06
**ステータス**: 実験中 - 辞書ベースアプローチ評価フェーズ 🧪
