# 偏旁自動検出実験プログラム

## 概要

このプログラムは、漢字から偏旁を自動的に検出・抽出する実験用ツールです。
実用レベルに達したら本体のフォントエディタに統合する予定です。

## 実装されている手法

### 1. 連結成分分析 (Connected Component Analysis)
- 文字を独立した塊（成分）に分解
- 各成分の位置・サイズから偏旁タイプを推定
- **利点**: シンプルで高速、縦書き/横書き偏旁の分離に有効
- **欠点**: 接触している偏旁は分離困難

### 2. テンプレートマッチング (準備済み)
- 既知の偏旁をテンプレートとして登録
- 正規化相互相関(NCC)でマッチング
- スケール変換に対応
- **利点**: 既知の偏旁を高精度で検出
- **欠点**: 未知の偏旁は検出不可

### 3. 複数文字からの共通部分抽出 (準備済み)
- 複数の漢字から共通の部分を見つける
- 新しい偏旁の発見に有効
- **利点**: 人手のアノテーション不要
- **欠点**: 複数の類似文字が必要

## インストール

### 必要なライブラリ

```bash
pip install pillow numpy scipy
```

## 使い方

### 基本的な流れ

1. **プログラム起動**
   ```bash
   python radical_auto_detect_experiment.py
   ```

2. **フォント読み込み**
   - メニュー → ファイル → フォントを開く
   - .ttf または .otf ファイルを選択

3. **文字を入力**
   - 左上の「文字」欄に漢字を1文字入力（例: 語、明、森）
   - 「レンダリング」ボタンをクリック

4. **パラメータ調整**
   - **2値化閾値**: 文字の境界を決める閾値（デフォルト200）
     - 低い値 → より多くの部分を黒として認識
     - 高い値 → より厳格に黒い部分のみ認識

   - **最小サイズ**: 成分として認識する最小ピクセル数（デフォルト100）
     - 小さい値 → 小さなノイズも検出
     - 大きい値 → 大きな成分のみ検出

5. **分析実行**
   - 「連結成分分析を実行」ボタンをクリック
   - 右側に検出された成分が表示される

6. **結果の確認**
   - 各成分の位置（left, right, top, bottom等）
   - 推定される偏旁タイプ（left_right, top_bottom等）
   - 成分の画像プレビュー

7. **成分の保存**
   - 各成分の「この成分を保存」ボタンで個別保存
   - assets/parts フォルダに保存すると本体で使用可能

## 実験の進め方

### Phase 1: 基本的な分割の検証

#### 実験1: 左右分割（偏・旁）
```
テスト文字: 語、話、時、明、晴、校、林、村
期待結果: 左成分と右成分に分割される
```

**確認ポイント**:
- 2成分に分割されるか？
- 左成分と右成分が正しく識別されるか？
- 接触部分で分離失敗していないか？

#### 実験2: 上下分割（冠・脚）
```
テスト文字: 空、家、字、安、宝、雲、雪
期待結果: 上成分と下成分に分割される
```

**確認ポイント**:
- 2成分に分割されるか？
- 上下の位置が正しく識別されるか？

#### 実験3: 複雑な構造
```
テスト文字: 森、品、晶、轟
期待結果: 3成分以上に分割される
```

**確認ポイント**:
- 繰り返しパターンが正しく認識されるか？

### Phase 2: パラメータのチューニング

#### チューニング項目

1. **2値化閾値の最適値を見つける**
   - フォントの太さによって最適値が異なる
   - 細いフォント: 180-200
   - 太いフォント: 200-220

2. **最小サイズの調整**
   - 小さすぎる: ノイズを誤検出
   - 大きすぎる: 小さな偏旁を見逃す
   - 推奨: 50-200の範囲で調整

### Phase 3: 成功パターンの蓄積

成功した分割結果を記録：

```json
{
  "character": "語",
  "font": "源ノ角ゴシック",
  "threshold": 200,
  "min_size": 100,
  "components": [
    {
      "position": "left",
      "saved_as": "言.png",
      "notes": "偏として使用可能"
    },
    {
      "position": "right",
      "saved_as": "吾.png",
      "notes": "旁として使用可能"
    }
  ],
  "success": true
}
```

## 判定基準

### 実用レベルの定義

以下の基準を満たせば本体統合を検討：

✅ **必須基準**:
1. 左右分割の成功率 > 80%（20文字中16文字以上）
2. 上下分割の成功率 > 70%（20文字中14文字以上）
3. 誤検出率 < 10%（間違った分割が10%未満）

✅ **推奨基準**:
1. パラメータ調整で複数のフォントに対応可能
2. 保存した偏旁が本体で正常に使用可能
3. ユーザーが手動で修正する必要が少ない

## 次のステップ

### 改善案

1. **エッジ検出の追加**
   - Cannyエッジ検出で境界を明確化
   - 接触部分の分離精度向上

2. **スケルトン化**
   - 文字のストロークを骨格化
   - 交点検出で分割位置を推定

3. **機械学習の導入**
   - CNNで偏旁領域をセグメンテーション
   - 学習データの収集が課題

### 本体統合計画

実用レベルに達した場合：

1. `FontEditorApp`にメニュー項目を追加
   - ツール → 偏旁自動検出

2. `GlyphEditor`から直接実行
   - 現在編集中の文字から偏旁を抽出
   - 抽出結果を偏旁パレットに自動登録

3. バッチ処理機能
   - 複数文字をまとめて処理
   - 共通偏旁を自動抽出

## トラブルシューティング

### Q: 成分が検出されない
A: 2値化閾値を下げてみてください（200 → 180など）

### Q: ノイズが大量に検出される
A: 最小サイズを上げてください（100 → 200など）

### Q: 接触部分で分離できない
A: 現在の連結成分分析では限界があります。次のフェーズでエッジ検出やストローク分析を導入予定

### Q: 特定のフォントで失敗する
A: フォントごとにパラメータを調整する必要があります。成功したパラメータをプリセットとして保存する機能を検討中

## ライセンス

本体のフォントエディタと同じライセンスを適用

## 貢献

実験結果のフィードバック歓迎：
- 成功した文字とパラメータ
- 失敗した文字と原因
- 改善提案

---

**バージョン**: 0.1.0
**最終更新**: 2025-11-06
**ステータス**: 実験中 🧪
